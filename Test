-- Carrega a biblioteca de UI
local UILibrary = require(path.to.UILibrary)  -- Substitua 'path.to.UILibrary' pelo caminho real da biblioteca

-- Cria uma nova janela
local Window = UILibrary:MakeWindow({ Name = "Título da biblioteca", Title = "Poison Key System", HidePremium = false, SaveConfig = true, ConfigFolder = "PoisonKeySystem" })

-- Adiciona um botão "Pegar Key" com callback
UILibrary:AddButton(Window, "Pegar Key", function()
    print("Pegar Key clicado!")
    -- Aqui você pode adicionar a lógica para pegar a chave
end, {
    Size = UDim2.new(0.35, 0, 0.1, 0),
    Position = UDim2.new(0.1, 0, 0.5, 0),
    BackgroundColor = Color3.new(0.3, 0.3, 0.3),
    TextColor = Color3.new(1, 1, 0),
    BorderSize = 2,
    BorderColor = Color3.new(1, 1, 0),
    TextSize = 24,
    Font = Enum.Font.SourceSansBold
})

-- Adiciona um botão "Checar Key" com callback
UILibrary:AddButton(Window, "Checar Key", function()
    print("Checar Key clicado!")
    -- Aqui você pode adicionar a lógica para checar a chave
end, {
    Size = UDim2.new(0.35, 0, 0.1, 0),
    Position = UDim2.new(0.55, 0, 0.5, 0),
    BackgroundColor = Color3.new(0.3, 0.3, 0.3),
    TextColor = Color3.new(1, 1, 0),
    BorderSize = 2,
    BorderColor = Color3.new(1, 1, 0),
    TextSize = 24,
    Font = Enum.Font.SourceSansBold
})

-- Adiciona um TextBox com placeholder personalizado
local keyInput = UILibrary:AddTextBox(Window, "Digite sua chave...", {
    Size = UDim2.new(0.8, 0, 0.1, 0),
    Position = UDim2.new(0.1, 0, 0.35, 0),
    BackgroundColor = Color3.new(0.2, 0.2, 0.2),
    TextColor = Color3.new(1, 1, 1),
    TextSize = 20,
    Font = Enum.Font.SourceSans
})

-- Adiciona um Label
local infoLabel = UILibrary:AddLabel(Window, "Digite a chave e clique em 'Checar Key'", {
    Size = UDim2.new(1, 0, 0.1, 0),
    Position = UDim2.new(0, 0, 0.15, 0),
    TextColor = Color3.new(1, 1, 0),
    TextSize = 20,
    Font = Enum.Font.SourceSansBold
})
